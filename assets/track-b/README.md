# Track B 샘플 데이터

## sample-video.mp4

- **출처**: YouTube — "입력받은 점수로 '평균 계산기' 만들기 (실전 예제)"
- **길이**: 3분 14초
- **해상도**: 720p
- **용량**: 약 7.5MB
- **언어**: 한국어

### 포함된 엣지 케이스

| 엣지 케이스 | 설명 | 확인 구간 | 테스트할 모듈 |
|------------|------|---------|-------------|
| 화면 코드 — 함수 목록 | `input()`, `split()`, `int()`, `len()`, `print()` 코드가 화면에 표시됨. 발표자가 모든 함수를 빠짐없이 읽지는 않음 | 0:00~2:10 | 모듈 1: VLM 프레임 분석 |
| 화면 코드 — 변수 초기화 | `total = 0` 코드가 화면에 표시됨. 말로 언급하지 않음 | 1:20 | 모듈 1: VLM 프레임 분석 |
| 실행 화면 — 팝업 입력값 | 실행 시 팝업에 `70,80,90,100,99`를 입력하는 장면이 화면에만 표시됨 | 2:30 | 모듈 1: VLM 프레임 분석 |
| 기술 용어 혼용 | `split`, `input`, `score_list` 등 영어 변수명/함수명이 한국어 설명과 혼용 | 전구간 | 모듈 2: STT 후처리 |
| 화면 코드 — 연산 로직 | `average = total / len(score_list)` 코드 구문이 화면에 표시됨 | 1:20 | 모듈 1: VLM 프레임 분석 |

### 예시 질문

```
[화면]      "영상에서 사용된 파이썬 내장 함수를 모두 나열해줘" → 0:00~2:10, 정답: input/split/int/len/print
[화면]      "total 변수의 초기값은 무엇인가?" → 1:20(80초), 정답: 0
[화면]      "영상 실행 화면에서 직접 입력한 숫자들은?" → 2:30(150초) 팝업, 정답: 70,80,90,100,99
[기술용어]   "점수를 입력받을 때 사용한 구분자는?" → 0:00~0:30, 정답: 쉼표(,)
[화면]      "평균을 계산하는 코드는 어떻게 작성되어 있는가?" → 1:20(80초), 정답: average = total / len(score_list)
```

> `test_questions.json`에 위 질문들이 포함되어 있습니다. 영상을 직접 시청한 후, 화면에만 보이는 정보와 말로 설명하는 정보를 구분하여 질문을 조정하세요.
