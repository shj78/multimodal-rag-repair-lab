# 2026-04-19 작업 회고

## 오늘 한 일

### Vision 프롬프트 개선 및 모델 실험

**v6-talkshow-visual 프롬프트 추가**
- 영화 월드컵 같은 다인 대담·시각 자료 풍부 영상을 위한 범용 비전 프롬프트 신설
- `[인물] / [시각 자료] / [화면 텍스트] / [구도·액션]` 4개 섹션으로 구조화
- `CURRENT_VISION_VERSION`을 `v5-code → v6-talkshow-visual`로 전환
- baseline(v5-code) 0/3 → Step 2(v6-talkshow-visual) 2/3 통과

**모델 다운그레이드 실험 (gpt-5.4 → gpt-4o)**
- V1(펄프픽션 식별) 후퇴, V2(안경 카운트) 유지, V3(레보스키) 유지 → 통과율 2/3 → 1/3
- 카운트 태스크는 gpt-4o로도 가능, 식별 태스크는 gpt-5.4 필요로 판단
- "엉뚱한 영화로 확신 있게 답변"하는 새 failure mode 발견
- Track 3/4 실험은 gpt-5.4 유지로 결론

**Vision concurrency 조정**
- gpt-4o 전환 시 4-worker 동시 호출이 OpenAI TPM rate limit 초과로 ingest 실패
- concurrency 4 → 2로 하향, 안정성 우선


---

### 시멘틱 청킹 구현

Track 2까지는 고정 30초 윈도우로 청킹했는데, baseline 분석에서 토픽 경계와 어긋나는 케이스가 반복 확인됐다 (C1, C4).

**Phase 1 — 스캐폴딩**
- `Config` / `EmbeddingCfg`에 `chunking_strategy`, `semantic_breakpoint_percentile`, `semantic_min_chunk_seconds` 3필드 추가
- 기본값 `fixed`라서 기존 실험 경로 동작 불변
- `chunk_segments()`에 strategy 분기 추가, 기존 sliding window는 `_chunk_fixed()`로 추출

**Phase 2 — 실구현**
- segment 임베딩 → 인접 코사인 거리 → percentile 기준 경계 판정 → min_seconds 제약 후 병합
- helper 4개 분리 추출 (`_adjacent_cosine_distances`, `_cosine_similarity`, `_percentile`, `_merge_by_boundaries`)
- numpy 없이 stdlib `math`만 사용, 스모크 테스트 4종 통과
- `embed_segment_for_boundary` 래퍼 추가 — chunk 임베딩과 맥락 분리

**버그 픽스**
- `get_config_snapshot()`이 신규 3필드를 내보내지 않아 실험 결과 JSON에 청킹 전략 정보가 빠지는 문제 수정. "기록 = 실행" 원칙 위반이라 즉시 수정.

---

### 데모 사이트 + 하네스 정비

- 포트폴리오 데모 사이트(`demo-site/`) 커밋. Next.js 정적 사이트, 4개 실험 케이스 수록
- pre-commit hook에 `demo-site` ESLint + TypeScript 체크 추가. `.ts`/`.tsx` 변경 시 자동 실행

---

## 내일 할 일 (선결 과제)

- 메타데이터에 pitch/감정 정보를 추가한 실험 진행
- baseline JSON과 시멘틱 결과 비교
